@model (string?,bool)

@if(Model.Item1!=null)
{
    if (Model.Item2 == true)
        await Html.RenderPartialAsync("Success", Model.Item1);
    else
        await Html.RenderPartialAsync("Failed", Model.Item1);
}