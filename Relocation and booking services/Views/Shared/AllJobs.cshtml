@model (List<Job>,List<IndustryUser>)

<div class="d-flex justify-content-center p-5">
    <input class="form-control w-25" list="cities" id="chosenCity" placeholder="Cities available" oninput="FilterOffers('jobForm',this.value)">
    <datalist id="cities">
        @foreach (string? company in Model.Item2.Select(item => item.CompanyName).Distinct().ToList())
        {
            <option value="@company" />
        }
    </datalist>
</div>

<form id="jobForm" asp-controller="User" asp-action="FoundJob">
    <div class="card-group w-50 h-50 p-5">
        @foreach (Job ap in Model.Item1)
        {
            <div class="card">
                <img src="@("data:image/jpg;base64,"+Convert.ToBase64String(ap.Image))" class="card-img-top" />
                <div class="card-body">
                    <h4 class="card-title">@ap.Name</h4>
                    <input class="form-control" type="text" value="@ap.CreatorId" name="jobCreatorId" hidden />
                    <input class="form-control" type="text" value="@ap.Id" name="jobId" hidden />
                    <p class="card-text">@ap.Description</p>
                    <p class="card-text">Company @(Model.Item2.First(item => item.UserId == ap.CreatorId).CompanyName)</p>
                    <div class="card-footer">
                        <p class="card-text">@ap.Price</p>
                        <a class="card-text" href="@ap.Link" target="_blank">Visit</a>
                    </div>
                </div>
                <div class="card-footer">
                    <blockquote class="blockquote">
                        Posted at @ap.Date?.ToString("dd.MM.yyyy HH:mm");
                    </blockquote>
                    <button class="btn btn-primary" type="button" onclick="MakeInputCommand('@ap.Id',document.getElementById('jobForm'),'@ap.CreatorId')">Apply</button>
                </div>
            </div>
        }
    </div>
</form>